# AIWS V2 Replay Record

- Project: `prj_1e7d1b1c926f4df69c` (`DesignSignal V5 Replay`)
- Workflow: `wfl_09c498f416f54fedb1`
- Planning quality: `verified`
- Initial context selection: `csel_17e32ba290944209b3`
- Initial useful document version: `ctxver_c3535dc68b449701a224bfd1`

The initial Context Selection pinned the approved zero-dependency architecture material. It excluded the controlled decoy and conflicting guidance by user policy, excluded a deleted old proposal as stale, and excluded a fixture in another project as cross-scope. Implementation began from an empty directory after that selection.

The old DesignSignal implementation is not an input to this repository. It may be inspected only after a fixed candidate SHA passes independent verification, for a blind outcome comparison.
